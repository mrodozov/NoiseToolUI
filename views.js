// Single run inspection section

var RunlistView = Backbone.View.extend ({
	// sort by 1. number 2. Type 3. Duration 
	
});

var RunBasicInfoView = Backbone.View.extend ({
	// single run details popup
});

var RunItemsView = Backbone.View.extend ({
	// dead, masked , etc. 
});

var RollListView = Backbone.View.extend ({
	// roll name only button
});

var RollDetailsView = Backbone.View.extend ({
	// based on selected runitem id , should show avg rate, total number of strips with condition, etc
});

var RollPlotView = Backbone.View.extend ({
	// strip rates as distribution, by numbers etc
});


// Run range inspection 

var RangeSelectionView = Backbone.View.extend ({
	
});

